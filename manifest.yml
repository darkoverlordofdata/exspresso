---
applications:
  .:
    name: exspresso
    framework:
      name: node
      info:
        mem: 128M
        description: Node.js Application
        exec: 
    infra: aws
    url: ${name}.${target-base}
    mem: 128M
    instances: 1
    services:
      d16a_postgresql:
        type: postgresql
